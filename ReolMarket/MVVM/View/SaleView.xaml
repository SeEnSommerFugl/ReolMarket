<Page x:Class="ReolMarket.MVVM.View.SaleView"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:ReolMarket"
      mc:Ignorable="d" 
      d:DesignWidth="1652" d:DesignHeight="1048"
      Title="SaleView">

    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- row 4 mid row-->
            <RowDefinition Height="12"/>    <!--spacing-->
            <RowDefinition Height="*"/>     <!-- fill -->
        </Grid.RowDefinitions>

        <!-- Row 1: filter bar -->
        <Border Style="{StaticResource Card}">
            <DockPanel LastChildFill="True">
                <!-- buttons? -->
                <StackPanel DockPanel.Dock="Right" Orientation="Horizontal" VerticalAlignment="Center">
        <!--<   Button Content="Ryd filtre" Command="{Binding ClearFiltersCommand}"
                            Style="{StaticResource ButtonBase}" Height="34" Margin="12,0,0,0"/>
                <Button Content="Opdater" Command="{Binding RefreshCommand}"
                            Style="{StaticResource ButtonPrimary}" Height="34" Margin="12,0,0,0"/>  -->
                </StackPanel>

                <!-- left title -->
                <TextBlock DockPanel.Dock="Left" Text="Salg" Style="{StaticResource H1}"
                   VerticalAlignment="Center" Margin="0,0,12,0"/>

                <!-- ComboBoxes -->
                    <TextBlock  Text="Måned:" Style="{StaticResource LabelMuted}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox   ItemsSource="{Binding Months}" 
                                HorizontalAlignment="Left"
                                SelectedIndex="{Binding SelectedMonth, Mode=TwoWay}"
                                Height="34" Width="120" Margin="0,0,12,0"/>

                    <TextBlock Text="År:" Style="{StaticResource LabelMuted}" VerticalAlignment="Center" Margin="0,0,6,0"/>
                    <ComboBox   ItemsSource="{Binding Years}" 
                                HorizontalAlignment="Left"
                                SelectedItem="{Binding SelectedYear, Mode=TwoWay}"
                                Height="34" Width="100"/>
            </DockPanel>
        </Border>

        <!-- Row 2: top row -->
        <Grid Grid.Row="2">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>   <!-- venstre kolonne -->
                <ColumnDefinition Width="12"/>  <!-- spacing -->
                <ColumnDefinition Width="*"/>   <!-- højre kolonne -->
            </Grid.ColumnDefinitions>

            <Grid.RowDefinitions>
                <RowDefinition Height="4*"/>    <!-- øverste sektion (stor) -->
                <RowDefinition Height="12"/>    <!-- spacing-->
                <RowDefinition Height="*"/>     <!-- nederste sektion (mindre) -->
            </Grid.RowDefinitions>

            <!-- LEFT COLUMN -->
            <Grid Grid.Column="0">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*"/>    <!-- registrer salg -->
                    <RowDefinition Height="12"/>    <!-- spacing-->
                    <RowDefinition Height="*"/>     <!-- Salgs-tal -->
                </Grid.RowDefinitions>

                <!-- Registrer salg (RecordSales)-->
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Registrer salg" Style="{StaticResource H2}" 
                                   HorizontalAlignment="Center"/>
                        <TextBlock Text="Reol" 
                                   HorizontalAlignment="Left" 
                                   Foreground="White" />
                        <TextBox/>
                        <TextBlock Text="Produkt" 
                                   HorizontalAlignment="Left" 
                                   Foreground="White"/>
                        <TextBox/>
                        <TextBlock Text="Pris" 
                                   HorizontalAlignment="Left" 
                                   Foreground="White"/>
                        <TextBox/>


                        <DataGrid ItemsSource="{Binding RecordSales}"
                                  HeadersVisibility="None"
                                  GridLinesVisibility="None" 
                                  Background="Transparent" 
                                  BorderBrush="Transparent"
                                  Margin="0,8,0,0"> 
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding Label}" Width="*" />
                                <DataGridTextColumn Binding="{Binding Value}" Width="140"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- Salgs-tal (SalesFigures) -->
                <Border Grid.Row="2" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock Text="Salgstal" Style="{StaticResource H2}" 
                                   HorizontalAlignment="Center"/>
                        <DataGrid  ItemsSource="{Binding SalesFigures}"
                                   HeadersVisibility="None"
                                   GridLinesVisibility="None" 
                                   Background="Transparent" 
                                   BorderBrush="Transparent"
                                   Margin="0,8,0,0"> 
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding Label}" Width="*"/>
                                <DataGridTextColumn Binding="{Binding Value}" Width="140"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>

            <Grid Grid.Column="2">
                <Grid.RowDefinitions>
                    <RowDefinition Height="2*" />   <!-- Udestående differencer -->
                    <RowDefinition Height="12"/>    <!-- spacing -->
                    <RowDefinition Height="*"/>     <!-- Reol-tal -->               
                </Grid.RowDefinitions>

                <!-- Udestående differencer (OutstandingDifferences) -->
                <Border Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock  Text="Udestående Differencer" Style="{StaticResource H2}" 
                                    HorizontalAlignment="Center"/>
                        <DataGrid   ItemsSource="{Binding OutstandingDifferences}"
                                    HeadersVisibility="None"
                                    GridLinesVisibility="None" 
                                    Background="Transparent" 
                                    BorderBrush="Transparent"
                                    Margin="0,8,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn 
                                    Binding="{Binding RenterName}" 
                                    Width="*" />
                                <DataGridTextColumn 
                                    Binding="{Binding Amount}" 
                                    Width="140">
                                </DataGridTextColumn>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>

                <!-- Reol-tal -->
                <Border Grid.Row="2" Style="{StaticResource Card}">
                    <StackPanel>
                        <TextBlock  Text="Reol tal" Style="{StaticResource H2}" 
                                    HorizontalAlignment="Center"/>
                        <DataGrid   ItemsSource="{Binding BoothFigures}" 
                                    HeadersVisibility="None"
                                    GridLinesVisibility="None" 
                                    Background="Transparent" 
                                    BorderBrush="Transparent"
                                    Margin="0,8,0,0">
                            <DataGrid.Columns>
                                <DataGridTextColumn Binding="{Binding Label}" Width="*" />
                                <DataGridTextColumn Binding="{Binding Value}" Width="140"/>
                            </DataGrid.Columns>
                        </DataGrid>
                    </StackPanel>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Page>
