<ResourceDictionary xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <!-- ===== Design Tokens ===== -->
    <!-- UI Coloring sceme -->
    <Color x:Key="Col.Surface">#FFF7F7F8</Color>
    <Color x:Key="Col.Panel">#FFFFFFFF</Color>
    <Color x:Key="Col.Border">#FFE5E7EB</Color>
    <Color x:Key="Col.Text">#FF111827</Color>
    <Color x:Key="Col.Muted">#FF6B7280</Color>

    <Color x:Key="Col.Primary">#FF3B82F6</Color>
    <Color x:Key="Col.PrimaryHover">#FF2563EB</Color>
    <Color x:Key="Col.Accent">#FF10B981</Color>
    <Color x:Key="Col.Warn">#FFEF4444</Color>

    <SolidColorBrush x:Key="Brush.Surface" Color="{StaticResource Col.Surface}" />
    <SolidColorBrush x:Key="Brush.Panel"   Color="{StaticResource Col.Panel}" />
    <SolidColorBrush x:Key="Brush.Border"  Color="{StaticResource Col.Border}" />
    <SolidColorBrush x:Key="Brush.Text"    Color="{StaticResource Col.Text}" />
    <SolidColorBrush x:Key="Brush.Muted"   Color="{StaticResource Col.Muted}" />

    <SolidColorBrush x:Key="Brush.Primary"       Color="{StaticResource Col.Primary}" />
    <SolidColorBrush x:Key="Brush.PrimaryHover"  Color="{StaticResource Col.PrimaryHover}" />
    <SolidColorBrush x:Key="Brush.Accent"        Color="{StaticResource Col.Accent}" />
    <SolidColorBrush x:Key="Brush.Warn"          Color="{StaticResource Col.Warn}" />

    <!-- ===== Typografi ===== -->
    <Style TargetType="TextBlock" x:Key="H1">
        <Setter Property="FontSize" Value="20"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="H2">
        <Setter Property="FontSize" Value="18"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
    </Style>

    <Style TargetType="TextBlock" x:Key="LabelMuted">
        <Setter Property="FontSize" Value="12"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Muted}"/>
    </Style>

    <!-- ===== Knapper ===== -->
    <Style TargetType="Button" x:Key="Btn.Base">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Text}"/>
        <Setter Property="Padding" Value="10,8"/>
        <Setter Property="Cursor" Value="Hand"/>
        <Setter Property="Margin" Value="0,6,0,0"/>
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="Template">
            <Setter.Value>
                <ControlTemplate TargetType="Button">
                    <Border Background="{TemplateBinding Background}"
                  BorderBrush="{TemplateBinding BorderBrush}"
                  BorderThickness="{TemplateBinding BorderThickness}"
                  CornerRadius="8">
                        <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                    </Border>
                </ControlTemplate>
            </Setter.Value>
        </Setter>
        <Setter Property="SnapsToDevicePixels" Value="True"/>
    </Style>

    <Style TargetType="Button" x:Key="Btn.Primary" BasedOn="{StaticResource Btn.Base}">
        <Setter Property="Background" Value="{StaticResource Brush.Primary}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Primary}"/>
        <Setter Property="Foreground" Value="White"/>
        <Style.Triggers>
            <Trigger Property="IsMouseOver" Value="True">
                <Setter Property="Background" Value="{StaticResource Brush.PrimaryHover}"/>
                <Setter Property="BorderBrush" Value="{StaticResource Brush.PrimaryHover}"/>
            </Trigger>
            <Trigger Property="IsEnabled" Value="False">
                <Setter Property="Opacity" Value="0.6"/>
            </Trigger>
        </Style.Triggers>
    </Style>

    <Style TargetType="Button" x:Key="Btn.Danger" BasedOn="{StaticResource Btn.Base}">
        <Setter Property="Background" Value="{StaticResource Brush.Warn}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Warn}"/>
        <Setter Property="Foreground" Value="White"/>
    </Style>

    <!-- Venstre navigation -->
    <Style TargetType="Button" x:Key="Btn.Nav" BasedOn="{StaticResource Btn.Base}">
        <Setter Property="HorizontalAlignment" Value="Stretch"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
    </Style>

    <!-- ===== DataGrid ===== -->
    <Style TargetType="DataGrid" x:Key="Grid.Base">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="RowBackground" Value="{StaticResource Brush.Panel}"/>
        <Setter Property="AlternatingRowBackground" Value="#FFF9FAFB"/>
        <Setter Property="HeadersVisibility" Value="Column"/>
        <Setter Property="GridLinesVisibility" Value="None"/>
        <Setter Property="CanUserAddRows" Value="False"/>
        <Setter Property="CanUserDeleteRows" Value="False"/>
        <Setter Property="AutoGenerateColumns" Value="False"/>
        <Setter Property="RowHeight" Value="36"/>
        <Setter Property="ColumnHeaderHeight" Value="34"/>
    </Style>

    <Style TargetType="DataGridColumnHeader">
        <Setter Property="Background" Value="#FFF3F4F6"/>
        <Setter Property="Foreground" Value="{StaticResource Brush.Muted}"/>
        <Setter Property="FontWeight" Value="SemiBold"/>
        <Setter Property="Padding" Value="8,0"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="0,0,0,1"/>
    </Style>

    <!-- ===== Difference ===== -->

    <!-- POSITIVE DIFFERENCE -->
    <Style TargetType="Border" x:Key="Chip.Positive">
        <Setter Property="Background" Value="#E8FAF2"/>
        <Setter Property="BorderBrush" Value="#B8F0D2"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="6"/>
        <Setter Property="Padding" Value="8,2"/>
    </Style>

    <!-- NEGATIV DIFFERENCE -->
    <Style TargetType="Border" x:Key="Chip.Negative" BasedOn="{StaticResource Chip.Positive}">
        <Setter Property="Background" Value="#FDECEC"/>
        <Setter Property="BorderBrush" Value="#F6C7C7"/>
    </Style>

    <!-- ===== Kort/panel ===== -->
    <Style TargetType="Border" x:Key="Card">
        <Setter Property="Background" Value="{StaticResource Brush.Panel}"/>
        <Setter Property="BorderBrush" Value="{StaticResource Brush.Border}"/>
        <Setter Property="BorderThickness" Value="1"/>
        <Setter Property="CornerRadius" Value="10"/>
        <Setter Property="Padding" Value="16"/>
    </Style>
</ResourceDictionary>
